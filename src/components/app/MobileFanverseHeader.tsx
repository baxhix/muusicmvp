/**
 * Header mobile — VARIAÇÃO 2 (A/B).
 *
 * Por decisão de produto, a Opção 2 NÃO tem header: nada é renderizado
 * no topo (sem imagem, sem textos, sem orbe). Mantido como componente
 * (em vez de tirar do switch do MobileHeaderAB) pra preservar o A/B com
 * a Opção 1.
 */
export default function MobileFanverseHeader() {
  return null;
}
